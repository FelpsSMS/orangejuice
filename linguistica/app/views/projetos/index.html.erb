<h1>Listing projetos</h1>

<table>
  <thead>
    <tr>
      <th>Projeto titulo</th>
      <th>Projeto descricao</th>
      <th>Pesquisador</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projetos.each do |projeto| %>
      <tr>
        <td><%= projeto.projeto_titulo %></td>
        <td><%= projeto.projeto_descricao %></td>
        <td><%= projeto.pesquisador %></td>
        <td><%= link_to 'Show', projeto %></td>
        <td><%= link_to 'Edit', edit_projeto_path(projeto) %></td>
        <td><%= link_to 'Destroy', projeto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Projeto', new_projeto_path %>
